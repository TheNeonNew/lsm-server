{
  "name": "lsm-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "module": "src/server.ts",
  "dependencies": {
    "cli": "^1.0.1",
    "codex": "^0.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "postman": "^0.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "bun-types": "^1.3.10",
    "jest": "^30.4.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "start": "NODE_ENV=production bun src/server.ts",
    "dev": "bun --watch src/server.ts",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "migrate": "bun src/db/runMigrations.ts"
  }
}
